\block{Solution}{
    \begin{enumerate}
        \item Create the 1st \textit{Peano} application from one extension, e.g. \textit{Multigrid}, following the same workflow as above
        \item Create the 2st \textit{Peano} application from another extension, e.g. \textit{ExaHyPE} in a similar fasion
        \item Merge the above 2 \texttt{peano4} Projects (the new feature of \textit{Peano4} API)
    \end{enumerate}
}